{
  "name": "school-elections-system",
  "version": "1.0.0",
  "description": "School Elections Management System",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run start",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node backend/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
